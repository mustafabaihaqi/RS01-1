import React from 'react';

const DoctorSchedulePage = () => {
  return (
    <div style={styles.container}>
      <h2>Jadwal Dokter</h2>
      {/* Tambahkan konten jadwal dokter di sini */}
    </div>
  );
};

const styles = {
  container: {
    padding: '20px',
    maxWidth: '800px',
    margin: '0 auto',
  }
};

export default DoctorSchedulePage;